function View(suffix, vars, theme) {
    this.suffix = suffix;
    this.vars = vars != undefined ? vars : {};
    this.theme = theme != undefined ? theme.toLowerCase() : undefined;
    this.index = "";
}
module.exports = View;